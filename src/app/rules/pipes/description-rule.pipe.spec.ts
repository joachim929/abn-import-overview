import { DescriptionRulePipe } from './description-rule.pipe';

describe('DescriptionRulePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionRulePipe();
    expect(pipe).toBeTruthy();
  });
});
